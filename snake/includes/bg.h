#include <vector>

void define_limits(std::vector< std::vector<int>> &bg,int bg_width,int bg_len);
void snake_init_background(const int snake_len, const int bg_width, const int bg_len, std::vector < std::vector<int> > &bg);
